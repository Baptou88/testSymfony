{% extends 'base.html.twig' %}

{% block title %}HeureProjet{% endblock %}

{% block body %}
    <h1>HeureProjet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ heure_projet.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ heure_projet.Date ? heure_projet.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ heure_projet.duree }}</td>
            </tr>
            <tr>
                <th>Employe</th>
                <td>{{ heure_projet.employe.email }}</td>
            </tr>
            <tr>
                <th>IsHidden</th>
                <td>{{ heure_projet.isHidden ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('heure_projet_index') }}">back to list</a>

    <a href="{{ path('heure_projet_edit', {'id': heure_projet.id}) }}">edit</a>

    {{ include('heure_projet/_delete_form.html.twig') }}
{% endblock %}
