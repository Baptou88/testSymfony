{% extends 'base.html.twig' %}

{% block title %}TypeProjet{% endblock %}

{% block body %}
   <div class="container">
       <h1>Type Projet</h1>

       <table class="table">
           <tbody>
           <tr>
               <th>Id</th>
               <td>{{ type_projet.id }}</td>
           </tr>
           <tr>
               <th>Name</th>
               <td>{{ type_projet.Name }}</td>
           </tr>
           <tr>
               <th>IsVisible</th>
               <td>{{ type_projet.IsVisible ? 'Yes' : 'No' }}</td>
           </tr>
           </tbody>
       </table>

       <a href="{{ path('type_projet_index') }}" class="btn btn-secondary">back to list</a>

       <a href="{{ path('type_projet_edit', {'id': type_projet.id}) }}" class="btn btn-secondary">edit</a>

       {{ include('type_projet/_delete_form.html.twig') }}
   </div>
{% endblock %}
